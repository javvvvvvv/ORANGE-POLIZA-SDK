# Orange Poliza Engine — dependencias de la Fase 0 (el motor).
#
# Nota: la exportación final a .xls (backend/app/exporters/xls_writer.py)
# no usa ningún paquete de Python para convertir a BIFF8 — invoca
# LibreOffice (`soffice`) instalado en el sistema. Por eso no aparece
# aquí: es una dependencia de sistema, no de pip. El .bat de instalación
# (iniciar_orange_poliza.bat) revisa que esté presente y ayuda a
# instalarlo si falta.
#
# Cuando se conecte la Fase 3 (PDF/OCR), se agregará aquí pdfplumber.

pandas>=2.1,<3.0
openpyxl>=3.1,<4.0
stripe==9.8.0
Authlib==1.3.1
requests==2.31.0
